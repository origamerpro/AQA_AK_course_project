import { SALES_PORTAL_URL } from 'config/environment';
import { logStep } from 'utils/reporter.utils';
import { PageHolder } from './pageHolder.page';

export abstract class SalesPortalPage extends PageHolder {
  // Здесь разместить методы работы с хедером (задание для первого освободившегося)
}
